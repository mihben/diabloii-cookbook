import { IngredientPipe } from './property.pipe';

describe('IngredientPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientPipe();
    expect(pipe).toBeTruthy();
  });
});
