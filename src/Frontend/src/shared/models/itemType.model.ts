export class ItemType {
    id: string;
    name: string;
    group: string;

    constructor(id: string, name: string, group: string) { 
        this.id = id;
        this.name = name;
        this.group = group;
    }
}